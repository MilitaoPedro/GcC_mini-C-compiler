name: Build and Test

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Instalar flex e gcc
        run: sudo apt-get update && sudo apt-get install -y flex gcc

      - name: Compilar com flex
        run: |
          flex scanner.l
          gcc lex.yy.c -o scanner
          ./scanner < teste.txt
